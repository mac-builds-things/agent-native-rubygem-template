# frozen_string_literal: true

source "https://rubygems.org"

# All runtime + development dependencies are declared in the gemspec.
# Add anything you need *only* during local development below.
gemspec

# Optional: local development utilities
# gem "pry",       require: false   # REPL debugger
# gem "pry-byebug", require: false  # Step debugger
